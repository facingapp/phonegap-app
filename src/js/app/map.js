app.map = {

	pins: [],
	type: null,
	elm: document.getElementById('map_canvas'),
	obj: null,
	options: {
		height: null,
		diameter: 1000,
		atBottom: false,
		lat: null,
		lon: null
	},
	init: function()
	{
//		app.map.obj = plugin.google.maps.Map.getMap(app.map.elm);
//
//		app.map.obj.addEventListener(plugin.google.maps.event.MAP_READY, function(map) {
//			// The map is initialized, then show a map dialog
//			map.showDialog();
//		});
	}
};
